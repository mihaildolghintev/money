<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @expense_category.name %>
</p>

<div class='flex justify-center align-middle gap-3 mt-10'>
  <%= link_to 'Edit', edit_expense_category_path(@expense_category), class: 'hover:bg-yellow-200' %> |
  <%= link_to 'Back', expense_categories_path, class: 'hover:bg-yellow-200' %>
</div>
